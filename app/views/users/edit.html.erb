<div class="container">
  <div class="row">
    <div class="col-sm-12 col-lg-5 px-5 px-sm-0 mx-auto">
      <h2>User info</h2>
      
        <% if @user.errors.any? %>
      <%= @user.errors.count %>error prohibited this obj from being saved:error prohibited this obj from being saved:
      <ul>
        <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
      </ul>
      <% end %>
      

      <%= form_with model: @user,local:true do |f| %>
      <div class="form-group">
        <ladel for="inputname">Name</ladel>
        <%= f.text_field :name, class: "form-control" %>
      </div>

      <div class="form-group">
        <ladel for="inputimage">Image</ladel>
        <%= f.file_field :profile_image, class: "form-control-file" %>
      </div>

      <div class="form-group">
        <ladel for="inputintroduction">Introduction</ladel>
        <%= f.text_field :introduction, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= f.submit "Update User", class: "btn btn-secondary" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
