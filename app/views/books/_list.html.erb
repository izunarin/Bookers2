<p id="notice"></p>
<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <h2>User info</h2>
      <!--%= image_tag book.get_profile_image(100,100) %>-->
      <table class="table">
      <tbody>
        <tr>
          <th>name</th>
          <!--<th>%= book.user.name %></th>-->
        </tr>
        <tr>
          <th>introduction</th>
          <th></th>
        </tr>
      </tbody>
      </table>
      <div class="row">
        <!--%= link_to edit_user_path, class: "btn btn-outline-secondary btn-block" do %>-->
        <span><i class="fas fa-user-cog"></i></span>
        <!--% end %>-->
      </div>
      <%= form_wih model: @book do |f| %>
      <h2 class="mt3">New book</h2>
      <h2>Title</h2>
      <%= f.text_field :title %>
      <h2>Opinion</h2>
      <%= f.text_area :body %>
      
      <% f.submit "Create Book" %>
      <% end %>
    </div>
  </div>
</div>