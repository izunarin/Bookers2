<% book.each do |book| %>
<div>
  <%= link_to books_path(books_path.id) do %>
  <%= image_tag book.get_image %>
  <% end %>
  <p><h1>User info</h1><br>
  <%= image_tag book.user.get_profile_image(100,100) %></p>
  <p>name<%=book.user.name %></p>
  <p>introduction</p>
  <p><%= link_to "" esit_user_path<p> %>
  <h1>New book/<h1>
    <%= form_with model: Book.new |f| %>
    <h4>Title</h4>
    <%= f.text_field :title %>
    
    <h4>Opinion</h4>
    <%= f.text_area :body %>
    
    <%= f.submit "Create Book" %>
</div>